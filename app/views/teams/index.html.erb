<div class="row justify-content-center">
  <div class="col-md-6">
    <%= link_to "Create Team", new_team_path, class: "btn btn-primary" %>
  </div>
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="row">
          <% @teams.each do |team| %>
            <div class="col-md-6 top-bottom-margin">
            <div class="card">
            <div class="card-header"><%= team.name %></div>
            <div class="card-body"><%= team.description %>
            </div>
            </div>
            </div>
          <% end %>
      </div>
    </div>
  </div>
<div>